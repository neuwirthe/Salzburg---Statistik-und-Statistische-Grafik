---
title: "R Notebook"
output:
  html_document:
    df_print: paged
---


```{r}
library(tidyverse)
library(magrittr)
library(scales)
library(readxl)
library(pracma)
```

```{r}
load(file.path("Lottodata","lotto_aut.RData"))
```
```{r}
lotto_aut %>% names()
```

```{r}
lotto_aut %>% nrow()
```

Unser Datenbestand deckt `r lotto_aut %>% nrow()` Runden ab.


```{r}
lotto_aut %>%
  select(Rtot,Datum,Tipps,Wochentag) %>%
  ggplot(aes(x=Rtot,y=Tipps,color=Wochentag)) +
  geom_point() +
  facet_grid(Wochentag ~ .)
```


```{r}
lotto_aut %>%
  filter(JackpotDavor==0) %>%
  filter(Tipps <=30000000) %>%
  select(Rtot,Datum,Tipps,Wochentag) %>%
  ggplot(aes(x=Rtot,y=Tipps,color=Wochentag)) +
  geom_point(size=0.01) +
  geom_smooth() +
  facet_grid(Wochentag ~ .)
```

```{r}
probs <- 
  read_excel(file.path("Lottodata",
                       "Probs_EN.xlsx")) %>%
  mutate(probs=choose(6,gruen)*choose(1,gelb)*choose(38,rot)/choose(45,6)) %>%
  select(Rang,Name,probs)
probs
```

